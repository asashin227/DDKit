use_frameworks!

target 'DDKit_Example' do
  pod 'DDKit', :path => '../'

  target 'DDKit_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.0.0'
  pod 'Nimble', '~> 5.1.1'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
  end
end
